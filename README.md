# Before & after screenshots

Used to make before & after screenshots manually in a photo editor, but that takes surprisingly a lot of time. This tool allows you to paste or drag & drop images and then copy the output as a preformatted image.
